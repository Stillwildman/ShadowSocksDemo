<?xml version="1.0" encoding="utf-8"?>
<layout>

    <androidx.constraintlayout.widget.ConstraintLayout
        xmlns:android="http://schemas.android.com/apk/res/android"
        xmlns:app="http://schemas.android.com/apk/res-auto"
        android:layout_width="match_parent"
        android:layout_height="match_parent" >

        <androidx.appcompat.widget.AppCompatImageView
            android:id="@+id/button_power"
            android:layout_width="@dimen/poi_photo_edit_view_l"
            android:layout_height="0dp"
            android:layout_marginTop="@dimen/padding_size_super"
            android:padding="@dimen/font_text_size_xxl"
            style="@style/Widget.AppCompat.Button"
            app:srcCompat="@drawable/selector_power"
            android:background="@drawable/background_oval_stroke_grey"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintDimensionRatio="1:1"/>

        <include
            android:id="@+id/layout_nodeSelector"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            layout="@layout/include_node_selector"
            app:layout_constraintTop_toBottomOf="@+id/button_power"
            app:layout_constraintBottom_toBottomOf="parent"/>

    </androidx.constraintlayout.widget.ConstraintLayout>

</layout>